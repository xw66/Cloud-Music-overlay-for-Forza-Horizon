# 更新日志

## v3.1.0 - 2026-09-19

本版本完成了架构级现代化重构、进程通信加固以及核心技术债治理。

### 重构与优化

- **MVVM 全链路闭环架构**：引入微软官方 `CommunityToolkit.Mvvm`，实现 NowPlaying、FloatingSettings、ThemeSettings、Hotkey、RemoteControl、Logs、About 七大页面 ViewModels，全面解耦 UI 与状态，主窗口 code-behind 深度精简。
- **单实例命名管道 IPC 唤醒与托盘防假死**：引入 `SingleInstanceIpcService` 跨进程命名管道机制；二次启动自动穿透激活前台窗口并居中复位，解决多屏/托盘残留假死问题。
- **配置原子落盘与自愈容灾机制**：`OverlaySettingsService` 升级为物理刷盘 + `File.Move` 原子覆盖，并引入 `.bak` 镜像备份与损坏自动回退恢复。
- **主播放轮询与 UI 线程解耦**：彻底剥离高频 `DispatcherTimer`，改由后台 Task 与 `SemaphoreSlim` 事件唤醒驱动，CPU 占用降至 0.15% 极低水平。
- **诊断日志异步无锁 Channel 架构**：`DiagnosticService` 重构为基于 `System.Threading.Channels` 的批量异步生产者-消费者模式，杜绝日志磁盘 I/O 阻塞主线程。
- **引入 IoC / DI 依赖注入容器**：引入 `Microsoft.Extensions.DependencyInjection`，建立统一服务组合根 `AppServiceRegistration`，全面消除主窗口手动服务装配。
- **网易云内存探针 God Class 模块化拆分**：将原 1906 行巨石类拆分为纯策略层、Win32 虚拟内存底层互操作层、候选时钟过滤流水线与门面协调器。
- **集中式 HttpClient 连接池生命周期管理**：引入统一 `SocketsHttpHandler`（15分钟连接轮转周期响应 DNS 变更），解耦并支持全服务注入与 Mock 单元测试。
- **测试工程扩充**：单元测试增加至 293 项，全自动化验证 100% 绿灯通过。

## v3.0.2 - 2026-07-19

本节列出 v3.0.2 相较于 v3.0.0 的累计更新。

### 新增

- 新增网易云只读内存时间轴实验功能，用于在播放、暂停、快进、快退和拖动进度后重新定位歌词，不会修改网易云进程。
- 新增实验时间轴设置开关；关闭、读取失败或当前网易云版本不兼容时，歌词会自动使用本地计时。
- 新增播放器数据源健康状态，主界面会显示正常、降级或不可用，并在数据恢复后自动重新连接。
- 新增带播放来源、歌曲、播放位置和健康状态上下文的崩溃报告，便于定位闪退原因。
- 新增网易云客户端版本、主程序哈希及 `orpheus_ipc_window` 入口校验，为后续兼容性适配提供依据。

### 优化

- 统一网易云与 SMTC 播放器数据源接口，播放器读取改为后台快照，避免界面直接等待内存扫描或系统媒体会话。
- 连续读取失败时采用指数退避并自动重试，降低网易云未启动、切歌或数据暂时不可用时的资源占用。
- 网易云歌曲标题调整为 0.5 秒刷新；歌曲 ID、封面地址和时长优先直接读取网易云本地播放列表，减少切歌等待。
- 缓存同一首网易云歌曲的完整元数据，减少对大型本地播放列表的重复解析和封面读取。
- 网易云与 SMTC 调整为完全独立的数据渠道：网易云只读取本地数据、只读内存时间轴并使用网易云快捷键；SMTC 只读取和控制系统媒体会话，不再互相回退或借用播放状态。
- 播放器数据渠道设置明确标注网易云为默认渠道，并提示 QQ 音乐、Apple Music、酷狗等支持系统时间轴的播放器选择 SMTC；用户仍可随时手动切换。
- SMTC 歌曲信息读取失败进入退避时，仍会独立刷新有效时间轴，避免 Apple Music 等播放器的歌词停在第一句。
- 歌词解析支持一行多个 LRC 时间戳和出现在文件后部的 `[offset]` 标签，并减少重复正则创建。
- 只有歌曲或时间轴内容实际变化时才通知界面，降低无效刷新和后台开销。
- 拆分 `MainWindow` 的播放、设置、快捷键、遥控、生命周期和更新职责，便于后续维护。
- 项目升级到 .NET 10，并提供 win-x64、win-x86、win-arm64 自包含单文件版本。

### 修复

- 修复歌曲信息轮询与时间轴轮询互相推迟，导致切歌延迟或 SMTC 快进后不再同步的问题。
- 修复连续切歌时内存扫描阻塞歌曲上下文更新，导致随后快进仍无法同步的问题。
- 修复切歌后首次内存时间轴尚未建立时，本地计时被空快照暂停，导致歌词卡在第一句或第二句的问题。
- 修复网易云暂停或时间轴暂时不可用后，悬浮窗歌词仍继续前进的问题。
- 修复 Apple Music 提供有效 SMTC 时间轴时，歌词仍只显示第一句的问题。
- 恢复切歌展示时直接携带真实歌曲封面，不再先显示空白或占位封面。
- 修复时间轴候选误判导致歌词回退、重新从头计时的问题。
- 修复异常内存浮点值触发 `OverflowException` 并导致程序闪退的问题。
- 隔离后台未观察任务异常及可恢复的播放器读取异常，减少长时间运行闪退。
- 修复歌手名称中中文逗号、顿号等分隔符识别异常的问题。

### 界面

- 移除悬浮窗歌曲文字下方的灰色半透明背景，保留文字阴影以提升画面融合度。
- 增加实验时间轴及本地计时降级状态提示，并明确提示网易云快进、快退后可能需要一段时间重新同步。
- 将设置项从“歌词数据来源”调整为“播放器数据渠道”，明确两条渠道的用途和隔离关系。

### 兼容性

- 设置结构升级到 v2，旧版设置会自动迁移，无需重新配置。
- win-x86 版本不启用网易云内存时间轴，会自动使用本地歌词计时。
- SMTC 歌词同步仍依赖播放器提供的系统时间轴。

### 已知限制

- 网易云内存时间轴仍为实验功能；快进、快退或连续切歌后需要重新扫描，歌词可能过一段时间才能恢复到正确位置。
- 网易云客户端升级后，内存布局可能变化；不兼容时会自动降级为本地歌词计时。
- QQ 音乐、Apple Music、酷狗等播放器必须通过 SMTC 提供有效歌曲信息和时间轴，才能实现歌词同步。

### 验证

- Release 构建通过：0 个警告、0 个错误。
- 自动化测试 246 项全部通过。
- win-x64、win-x86、win-arm64 均生成 .NET 10 自包含单文件发布包。

## v3.0.0 - 2026-07-17

### 新增

- 新增手机遥控功能：电脑端内置局域网控制页，手机与电脑连接同一 Wi-Fi 后可扫码控制上一首、下一首、播放/暂停、显示/隐藏悬浮窗。
- 新增手机遥控设置页：支持启用/关闭服务、复制连接地址、显示二维码、重置连接令牌。
- 新增局域网地址优化：优先选择真实可用的 Wi-Fi/以太网 IPv4，过滤回环地址、无效地址和常见虚拟网卡地址，并显示备用地址。
- 新增 Cover Flow 模式：支持 3D 封面轮播显示。
- 新增方向盘等 HID/DirectInput 外设按钮支持，通用按钮名为 `Button1` 到 `Button16`。
- 新增悬浮窗隐藏快捷键，可通过键盘或手柄/方向盘外设控制显示与隐藏。

### 优化

- 强化悬浮窗置顶策略：切歌显示后短时间内高频刷新窗口 Z 顺序，并使用非激活工具窗口样式，提升 Minecraft、无边框全屏、窗口化全屏和普通置顶程序中的显示稳定性。
- 优化手机遥控连接体验：手机网页请求加入超时处理，连接失败时提示检查同一 Wi-Fi、防火墙和网络切换。
- 优化 SMTC 歌词同步、封面刷新和后台轮询策略，降低后台开销并提升切歌后的封面稳定性。
- 优化 DS5、Xbox 手柄和通用外设快捷键捕获，减少误触发和空闲状态误识别。
- 优化日志与诊断提示，便于排查播放器数据、网络、封面和歌词问题。

### 修复

- 修复手机遥控“复制地址”在剪贴板被占用时可能导致程序闪退的问题。
- 修复歌词服务重置后不复用同版本歌词缓存的问题。
- 修复部分 SMTC 场景下封面复用旧图或刷新时机不稳定的问题。
- 修复部分外设组合键捕获时基线状态干扰输入的问题。

### 已知限制

- DirectX 独占全屏游戏不支持普通桌面悬浮窗置顶。此类游戏会绕过桌面窗口合成，Windows 不允许普通 WPF/Win32 透明窗口覆盖在其画面上方。
- 对于 DX 独占全屏游戏，建议改用窗口化、无边框全屏、窗口化全屏模式，或使用 OBS 等直播软件的窗口源捕获方案。
- 手机遥控仅支持同一局域网内访问，不提供公网远程控制；若手机无法连接，请确认手机和电脑在同一 Wi-Fi，并允许 Windows 防火墙放行本程序。
