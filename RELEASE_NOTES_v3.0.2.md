# 网易云悬浮窗 v3.0.2 更新说明

本版本为相较于 v3.0.0 的累计稳定性、播放器兼容性和歌词同步更新。完整分类记录见 [CHANGELOG.md](CHANGELOG.md)。

## 主要更新

- 项目升级到 .NET 10，提供 win-x64、win-x86、win-arm64 自包含单文件版本，无需另外安装 .NET 运行时。
- 新增网易云只读内存时间轴实验功能，用于在播放、暂停和进度跳转后重新定位歌词；该功能不会修改网易云进程。
- 网易云与 SMTC 改为完全独立的数据渠道。网易云用户默认使用网易云专用渠道；QQ 音乐、Apple Music、酷狗等提供系统时间轴的播放器使用 SMTC，用户仍可自行切换。
- 优化网易云歌曲识别、封面读取、本地播放列表缓存和后台轮询，降低切歌延迟及重复解析开销。
- 修复 Apple Music 提供有效 SMTC 时间轴时歌词停在第一句，以及歌曲信息暂时读取失败时错误阻塞时间轴的问题。
- 修复多种时间轴回退、连续切歌、封面延迟、暂停后歌词继续推进及异常内存值导致闪退的问题。
- 歌词解析支持一行多个 LRC 时间戳及后置 `[offset]` 标签。
- 移除悬浮窗文字下方的灰色半透明背景。
- 增加播放器数据健康状态、兼容性诊断和崩溃上下文，便于排查客户端差异与长时间运行问题。

## 已知限制

- 网易云内存时间轴仍为实验功能。快进、快退或连续切歌后需要重新扫描，歌词可能过一段时间才能恢复到正确位置。
- 网易云客户端版本变化可能导致内存时间轴暂时不兼容，此时会自动降级为本地歌词计时。
- SMTC 歌词同步依赖播放器提供有效的歌曲信息和时间轴。
- win-x86 版本不支持网易云内存时间轴，会使用本地歌词计时。

## 验证

- Release 构建：0 个警告、0 个错误。
- 自动化测试：246 项全部通过。
- 已生成 win-x64、win-x86、win-arm64 三种架构的自包含单文件发布包。
